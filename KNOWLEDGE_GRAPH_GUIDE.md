# 知识图谱（Graph）任务全面升级指导方案（Zero-Token-Surge 版）

> **设计准则**：
> 1. **Token 零激增（< 2% 微增）**：不增加 LLM 调用轮次，单篇生成仅追加一个极短枚举字段（约 20~30 tokens），Prompt 采用**高密度精炼**替代冗长膨胀；
> 2. **执行耗时完全一致**：不引入多阶段模型交互，端到端耗时与现状一致；
> 3. **算力与推导前端化**：立体知识卡片、因果解读、推导链路、搜索过滤全部由 **Python / 前端 JS 确定性图算法计算（0 Token、0 延迟）**。

---

## 目录
- [一、核心设计哲学：Token 与耗时双约束下的破局点](#一核心设计哲学token-与耗时双约束下的破局点)
- [二、第一步（P0·极简高密）：关系语义公理化与轻量类型约束（抽取精度攻坚）](#二第一步p0极简高密关系语义公理化与轻量类型约束抽取精度攻坚)
  - [2.1 痛点精准拆解](#21-痛点精准拆解)
  - [2.2 高密度《语义方向公理表》（改写法不增 Token）](#22-高密度语义方向公理表改写法不增-token)
  - [2.3 极轻量节点元类型（全篇仅增 ~25 Tokens）](#23-极轻量节点元类型全篇仅增-25-tokens)
  - [2.4 淘汰“相关”垃圾桶，强化连通保底](#24-淘汰相关垃圾桶强化连通保底)
  - [2.5 Python 确定性清洗拦截（0 Token 纠偏）](#25-python-确定性清洗拦截0-token-纠偏)
- [三、第二步（P1·零消耗富交互）：纯前端拓扑推导引擎与立体知识卡片（展示深度攻坚）](#三第二步p1零消耗富交互纯前端拓扑推导引擎与立体知识卡片展示深度攻坚)
  - [3.1 零 Token 原理：由 JS 动态计算立体信息](#31-零-token-原理由-js-动态计算立体信息)
  - [3.2 四层立体知识卡片（类型/核心枢纽度/定义）](#32-四层立体知识卡片类型核心枢纽度定义)
  - [3.3 核心亮点：前端自动推导链路（先修必备 ➔ 实战应用 ➔ 易错警示）](#33-核心亮点前端自动推导链路先修必备--实战应用--易错警示)
  - [3.4 边点击：自然语言因果模板合成（0 Token 即时生成）](#34-边点击自然语言因果模板合成0-token-即时生成)
  - [3.5 双向漫游：点击邻居标签平滑飞入聚焦（Fly-to & Focus）](#35-双向漫游点击邻居标签平滑飞入聚焦fly-to--focus)
- [四、第三步（P2·纯客户端工具）：图谱交互工具箱与多视图切换（体验生态完善）](#四第三步p2纯客户端工具图谱交互工具箱与多视图切换体验生态完善)
  - [4.1 客户端即时模糊搜索框](#41-客户端即时模糊搜索框)
  - [4.2 章节 / 类型 / 核心骨架三重过滤](#42-章节--类型--核心骨架三重过滤)
  - [4.3 双布局秒级切换（CoSE 力导向 ⟷ Dagre 层级推导树）](#43-双布局秒级切换cose-力导向--dagre-层级推导树)
  - [4.4 增量记忆色彩光晕与成就反馈](#44-增量记忆色彩光晕与成就反馈)
- [五、Token 与耗时变化对照测算表](#五token-与耗时变化对照测算表)
- [六、分步实施清单与文件改造指引](#六分步实施清单与文件改造指引)

---

## 一、核心设计哲学：Token 与耗时双约束下的破局点

在保证 **LLM Token 消耗基本一致、运行时间完全一致** 的前提下，如何大幅提升图谱的准确度与交互深度？

```
┌────────────────────────────────────────────────────────────────────────┐
│                        双约束破局架构设计                              │
├──────────────────────────────────┬─────────────────────────────────────┤
│ 传统的重型做法（不可取 ❌）      │ 本方案的高效轻量做法（推荐 ✔）      │
├──────────────────────────────────┼─────────────────────────────────────┤
│ 多阶段 LLM 校验与反思 (耗时翻倍) │ 极简 Prompt 高密度公理引导 (耗时不变)│
│ LLM 输出大段自然语言解读 (Token飙升)│ 前端 JS 模板秒级因果合成 (0 Token消耗)│
│ LLM 推导知识前置后置依赖 (Token暴涨)│ 前端 Cytoscape 拓扑算法遍历 (0 Token消耗)│
│ 臃肿宽松的节点属性模式          │ 极简 1-word 节点元类型枚举 (~25 Tokens)│
└──────────────────────────────────┴─────────────────────────────────────┘
```

**三大底线保障**：
1. **调用链路不增**：依然维持 `graph_agent` (1次提取) $	o$ `graph_supervisor` (1次快审) $	o$ `materialize` (纯算法学习地图拼装)；
2. **输入 Context 不增**：沿用已裁剪好的 `note_purpose + sections + key_terms`；
3. **输出 Payload 极小化**：大模型仅输出极简三元组与节点属性，所有展示层的“解读”、“分析”、“推导链”全部下放至客户端算力。

---

## 二、第一步（P0·极简高密）：关系语义公理化与轻量类型约束（抽取精度攻坚）

> **目标**：以几乎**零新增 Token** 的代价，彻底根治“关系箭头倒置”、“相关泛滥”与“跨维度乱连边”。

### 2.1 痛点精准拆解
- **方向倒装的根源**：Prompt 只列了关系词，未定义**主谓宾的因果流向**，模型在 `[条件] -> 前提 -> [结论]` 和 `[结论] -> 前提 -> [条件]` 之间摇摆；
- **“相关”泛滥的根源**：Prompt 允许 `相关`，成为大模型不愿深度思考时的逃避选项；
- **乱连边的根源**：模型不知道节点是“公式”还是“题型”，造成方法和公式平级乱连。

### 2.2 高密度《语义方向公理表》（改写法不增 Token）
不用扩充篇幅，只需用**高密度表格**替换原 Prompt 中松散的口语说明，规定唯一的物理流动方向：

| 关系谓词 | 严格语法方向代入公式 | 极简标准正例（强制遵循） | 常见错误反例（严厉禁止） |
| :--- | :--- | :--- | :--- |
| **`前提`** | **条件/约束 $A$ 是 $B$ 成立或使用的必要前提** | `定义域对称` $	o$ `前提` $	o$ `奇偶性` | ❌ `奇偶性` $	o$ `前提` $	o$ `定义域对称` |
| **`用于`** | **方法/公式 $A$ 用来解决/化简题型/场景 $B$** | `换底公式` $	o$ `用于` $	o$ `对数化简求值` | ❌ `对数化简求值` $	o$ `用于` $	o$ `换底公式` |
| **`包含`** | **上位范畴/章节 $A$ 包含子范畴/要素 $B$** | `指数函数` $	o$ `包含` $	o$ `底数大于1的单调增` | ❌ `单调增` $	o$ `包含` $	o$ `指数函数` |
| **`转化`** | **复杂原式/问题 $A$ 变形为等价简化式 $B$** | `对数方程` $	o$ `转化` $	o$ `代数方程组` | ❌ 反向转化 |
| **`等价于`**| **命题/形态 $A$ 与 $B$ 充要等价** | `函数在I单调递增` $	o$ `等价于` $	o$ `导数≥0且不恒为0` | — |
| **`区别于`**| **易混淆概念 $A$ 与 $B$ 存在本质判定边界** | `单调区间` $	o$ `区别于` $	o$ `函数的定义域` | — |
| **`导致`** | **特定操作/疏忽 $A$ 引发结果/错误 $B$** | `忽略底数分类讨论` $	o$ `导致` $	o$ `增解或漏解` | — |

### 2.3 极轻量节点元类型（全篇仅增 ~25 Tokens）
在 [domain/notes/tasks/graph/contracts.py](file:///D:/AgentFlow/domain/notes/tasks/graph/contracts.py) 的 `nodes` 中追加一个字符极短的枚举字段：
```python
StrField("type", "节点类型: concept(概念)|formula(公式定理)|method(解法技巧)|problem(题型场景)|pitfall(易错条件)")
```
- **Token 影响**：单篇笔记通常提取 20~25 个节点，每个节点仅增加 1 个英文字词（如 `"method"`），整篇仅增加 **20~25 个 Tokens**（总输出约 1300 tokens，占比不足 1.8%）；
- **精度提升效果**：大模型拥有了 Schema 约束意识，`method/formula` 自动匹配 `--[用于]--> problem`，`pitfall` 自动匹配 `--[前提]--> formula/method`，从源头消灭跨界乱连。

### 2.4 淘汰“相关”垃圾桶，强化连通保底
- 从可选集合中**彻底删除 `相关`**，迫使模型给出确切的推导逻辑；如果原文只有模糊弱相关，宁可不连边；
- **保底连通规则**：对暂时没有上下游方法的孤立概念，直接建立 `所属章节 --[包含]--> 概念`，确保全图连通。

### 2.5 Python 确定性清洗拦截（0 Token 纠偏）
在 [domain/notes/tasks/graph/steps/graph_agent.py](file:///D:/AgentFlow/domain/notes/tasks/graph/steps/graph_agent.py) 的 `sanitize_graph` 中加入轻量 Python 校验函数（执行时间 < 1ms，0 Token）：
- 自动剔除自环（$A 	o A$）；
- 自动纠正反向重复边；
- 检查 `source` 与 `target` 是否存在；
- 若出现非法关系词，自动降级映射为合法谓词。

---

## 三、第二步（P1·零消耗富交互）：纯前端拓扑推导引擎与立体知识卡片（展示深度攻坚）

> **目标**：不向大模型索要多余文字，依靠**纯前端 JavaScript / CSS**，在点击节点或边时展示极其丰富、立体的知识推导卡片。

### 3.1 零 Token 原理：由 JS 动态计算立体信息
大模型只需老老实实输出极简的 `nodes` 和 `edges`，前端 Cytoscape.js 在页面加载完成的瞬间（< 5ms），通过内置图遍历算法动态生成衍生元数据：
- **节点的度数与核心枢纽度**：通过 `node.degree()` 计算，入度+出度高的自动标记为「核心知识枢纽」；
- **先修必备依赖（Prerequisites）**：遍历以当前节点为 `target` 且关系为 `前提`、`包含` 的所有入边，提取前置节点；
- **实战应用链路（Applications）**：遍历以当前节点为 `source` 且关系为 `用于`、`转化` 的所有出边，提取下游题型；
- **易错避坑警示（Pitfalls）**：检索相连节点中 `type === 'pitfall'` 的所有关联项。

### 3.2 四层立体知识卡片（类型/核心枢纽度/定义）
点击任意节点，右侧侧边栏即刻呈现分层清晰的现代卡片：
1. **Header 元信息区**：
   - 概念名称；
   - 类型 Badge（不同颜色：概念蓝、公式绿、解法紫、题型橙、易错红）；
   - 枢纽指标（如：`★ 核心枢纽 · 6条关联`）；
   - 增量标识（`新增` / `历史`）。
2. **定义 / 核心表达式区**：原文精准定义或关键公式提取。
3. **推导路径全景区（见 3.3）**。
4. **同章伙伴概念区**：显示本章节下的兄弟节点快捷标签。

### 3.3 核心亮点：前端自动推导链路（先修必备 ➔ 实战应用 ➔ 易错警示）
利用原生 JS 在侧边栏中渲染出类似工作流的链式布局：
```
┌────────────────────────────────────────────────────────┐
│ ✦ 知识推导路径 (由前端算法秒级渲染)                    │
├────────────────────────────────────────────────────────┤
│ ◲ 先修必备 (Prerequisites):                            │
│   [真数大于0]  [底数大于0且≠1]                        │
│                           ↓ (支撑前提)                 │
│ ◉ 当前知识点: 【换底公式】 (公式定理)                 │
│                           ↓ (实战应用)                 │
│ ◳ 实战应用 (Applications):                             │
│   ➔ [化同底对数求值] (题型)                           │
│   ➔ [对数方程转化] (题型)                             │
│                                                        │
│ ⚠ 易错警示 (Pitfalls):                                 │
│   ! [底数真数颠倒] (易错点)                            │
└────────────────────────────────────────────────────────┘
```

### 3.4 边点击：自然语言因果模板合成（0 Token 即时生成）
用户点击连线时，前端 JS 根据关系的动词自动套入自然语言解释模板，瞬间生成人类友好的大白话解读，**不需要模型多写一句话**：

```javascript
// 纯前端 0 Token 生成的高质量白话解读
function explainEdge(src, rel, tgt) {
  const templates = {
    '前提': `“【${src}】是【${tgt}】能够成立或使用的必要前置条件。”`,
    '用于': `“以【${src}】为核心解题工具/方法，用于攻克【${tgt}】。”`,
    '包含': `“【${src}】涵盖了【${tgt}】这一关键要素或子范畴。”`,
    '转化': `“将复杂的【${src}】等价转化为更易处理的【${tgt}】。”`,
    '等价于': `“【${src}】与【${tgt}】充要等价，二者可完全互推。”`,
    '区别于': `“【${src}】与【${tgt}】存在本质判定边界，解题时不可混淆。”`,
    '导致': `“若发生【${src}】，将直接引发【${tgt}】的计算或逻辑错误。”`
  };
  return templates[rel] || `“【${src}】与【${tgt}】通过【${rel}】紧密关联。”`;
}
```
同时下方优雅附上原文 `evidence` 原句和出处章节。

### 3.5 双向漫游：点击邻居标签平滑飞入聚焦（Fly-to & Focus）
- 侧边栏里出现的每一个前置概念、应用题型、易错警示标签，都绑定点击事件；
- 点击后，Cytoscape 调用平滑镜头移动：
  ```javascript
  cy.animate({
    center: { eles: targetNode },
    zoom: 1.4,
    duration: 400,
    easing: 'ease-in-out-cubic'
  });
  ```
- 目标节点瞬间高亮脉冲，侧边栏联动更新为目标节点的四层立体卡片。无需重新计算，纯浏览器交互体验。

---

## 四、第三步（P2·纯客户端工具）：图谱交互工具箱与多视图切换（体验生态完善）

> **目标**：不依赖后端，纯由前端 JS 注入轻量交互控件，提升复杂图谱的可用性。

### 4.1 客户端即时模糊搜索框
- 在画布顶部内嵌 30 行原生 JS 搜索组件；
- 用户键入关键字（如“换元”），自动模糊匹配所有节点的 `name` 和 `definition`；
- 命中的节点保持高光，其余节点透明度降为 0.1；回车直接居中聚焦。

### 4.2 章节 / 类型 / 核心骨架三重过滤
侧边栏下方提供轻量交互开关：
- **按类型过滤**：勾选框（`[✔] 概念` `[✔] 公式` `[✔] 方法` `[✔] 题型` `[✔] 易错`）。例如复习时取消勾选概念，**只看「方法 ➔ 题型」的刷题网络**；
- **骨架模式（Backbone）**：一键隐藏从属与细枝末节，只保留 `前提` 与 `用于` 主干链条。

### 4.3 双布局秒级切换（CoSE 力导向 ⟷ Dagre 层级推导树）
- **网状布局（CoSE）**：观察知识簇群的聚类关系；
- **推导树布局（Dagre/Breadthfirst）**：按因果和层级自顶向下单向排列，逻辑先后顺序极度直观。

### 4.4 增量记忆色彩光晕与成就反馈
结合 [tools/memory/graph.py](file:///D:/AgentFlow/tools/memory/graph.py)：
- 历史旧节点采用经典的低饱和度稳重灰蓝色；
- 本场新增节点带有亮丽的金琥珀色边框与轻微发光阴影；
- 顶部提供小按钮：**“只看本次新增节点”**，一键点亮本次学习收获。

---

## 五、Token 与耗时变化对照测算表

| 指标 | 改造前现状 | 优化后预测 | 净增变化 | 评估结论 |
| :--- | :---: | :---: | :---: | :--- |
| **LLM 调用次数** | 2 次（Agent + Supervisor） | 2 次（Agent + Supervisor） | **0 次** | 耗时基本完全一致 |
| **Prompt 输入 Tokens** | ~1450 tokens | ~1420 tokens（高密度精简） | **-30 tokens** | 甚至略有下降 |
| **Agent 输出 Tokens** | ~1300 tokens | ~1325 tokens（追加极短 type）| **+25 tokens** | 仅微增约 1.8% |
| **Supervisor 输入 Tokens**| ~1800 tokens | ~1825 tokens | **+25 tokens** | 忽略不计 |
| **端到端生成耗时** | ~6.5 秒 | ~6.6 秒 | **微增 < 0.1 秒** | 毫无感知差异 |
| **前端交互信息深度** | 纯文字单向列表 | **推导链+白话解读+飞入聚焦** | **质的飞跃** | 100% 前端算力承载 |

---

## 六、分步实施清单与文件改造指引

后续落地执行时，严格对照以下三步推进：

### 第一步：契约微增与 Prompt 高密度升级（P0·核心底座）
1. [contracts.py](file:///D:/AgentFlow/domain/notes/tasks/graph/contracts.py)：在 `nodes` 契约中追加 `type` 枚举字段；`edges` 中废除 `相关`；
2. [prompts.py](file:///D:/AgentFlow/domain/notes/tasks/graph/prompts.py)：替换为《高密度语义方向公理表》，精炼冗余指令；
3. [steps/graph_agent.py](file:///D:/AgentFlow/domain/notes/tasks/graph/steps/graph_agent.py)：在 `sanitize_graph` 强化自环纠错与类型映射；
4. [steps/graph_supervisor.py](file:///D:/AgentFlow/domain/notes/tasks/graph/steps/graph_supervisor.py)：注入方向反装审查规则。

### 第二步：纯前端推导算法与立体卡片（P1·交互质变）
1. [tools/exports/knowledge_graph.py](file:///D:/AgentFlow/tools/exports/knowledge_graph.py)：
   - 扩展节点元素数据：`type`、`degree`；
   - 注入前端推导链计算函数（提取 Prerequisites、Applications、Pitfalls）；
   - 注入白话逻辑解释合成器 `explainEdge()`；
   - 注入 `fly-to` 动画函数与交互式邻居标签；
   - 升级侧边栏现代学术/科技风格 CSS。

### 第三步：轻量客户端工具箱（P2·体验生态）
1. [tools/exports/knowledge_graph.py](file:///D:/AgentFlow/tools/exports/knowledge_graph.py)：
   - 注入纯前端即时搜索输入框与过滤监听；
   - 注入类型多选框与骨架视图切换；
   - 接入层级布局（Breadthfirst/Dagre）切换按钮。

---

*本方案由 AgentFlow 架构组制定，严格锁定在「Token 零激增、耗时完全一致、纯算力赋能」的技术轨道上。*
