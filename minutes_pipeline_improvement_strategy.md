# Minutes 会议纪要全链路改进策略清单（Prompt 与 template_v3 模板重构方案）

> **文档定位**：针对当前会议纪要输出“细节过多、流水账化、篇幅失控、耗时与 Token 偏高、偶发越权推断与盲猜人名”等痛点，制定的**端到端全链路改进策略清单**。涵盖篇幅预算层、草稿生成 Agent、监督审核 Supervisor、正文形态规则库、模板渲染器以及 `template_v3` 下全部 31 个场景模板的深度重构方案。  
> **执行约束**：本方案严格遵循“先深度思考分析、形成系统性策略清单，暂不修改生产代码与模板”的指令。

---

## 目录
1. [现状痛点深度诊断与根因分析](#一现状痛点深度诊断与根因分析)
2. [重构三大核心原则与规范基准](#二重构三大核心原则与规范基准)
3. [Minutes 全链路 Prompt 与逻辑层改进清单](#三minutes-全链路-prompt-与逻辑层改进清单)
   - 3.1 [篇幅预算层：`length_budget.py` 降载与机制松绑](#31-篇幅预算层length_budgetpy-降载与机制松绑)
   - 3.2 [草稿 Agent：`MINUTES_GENERATION_SYSTEM_PROMPT` 归纳授权与动词阶梯](#32-草稿-agentminutes_generation_system_prompt-归纳授权与动词阶梯)
   - 3.3 [监督审查层：`MINUTES_SUPERVISOR_DOMAIN_PROMPT` 边界卡控与极简留痕](#33-监督审查层minutes_supervisor_domain_prompt-边界卡控与极简留痕)
   - 3.4 [正文形态与渲染层：`body_rules.py` 与 `template_prompt.py` 树状层级规范](#34-正文形态与渲染层body_rulespy-与-template_promptpy-树状层级规范)
4. [`template_v3` 模板库分级优化方案](#四template_v3-模板库分级优化方案)
   - 4.1 [基石模板：`general_minutes.md`（通用纪要）重构设计](#41-基石模板general_minutesmd通用纪要重构设计)
   - 4.2 [核心模板：`personal_minutes.md`（个人视角纪要）重构设计](#42-核心模板personal_minutesmd个人视角纪要重构设计)
   - 4.3 [专业场景模板（29 个）：场景化分类与标准化对齐清单](#43-专业场景模板29-个场景化分类与标准化对齐清单)
5. [重构效果对比示范（Before vs. After）](#五重构效果对比示范before-vs-after)
6. [分阶段落地实施路线图与测试验收方案](#六分阶段落地实施路线图与测试验收方案)

---

## 一、现状痛点深度诊断与根因分析

经过对当前代码库、Prompt 体系及测试流的深入审计，现阶段会议纪要虽然实现了“内容全面覆盖、事实稳定还原”，但在实用性上偏离了纪要本质，主要根因如下：

```
┌────────────────────────────────────────────────────────────────────────┐
│                          当前纪要失衡的传导链条                         │
├────────────────────────────────────────────────────────────────────────┤
│ 【篇幅预算】TIERS 上限达 8000 字 ＋ "低于下限＝漏事实"恐吓式引导       │
│                                ↓                                       │
│ 【草稿 Prompt】"能写清就别合并"、"不得以预算为理由删事实"禁用归纳能力   │
│                                ↓                                       │
│ 【LLM 生成行为】将所有口语辩驳、探讨过程、琐碎铺垫逐一罗列（流水账化） │
│                                ↓                                       │
│ 【边界失控】为满足事实饱满，将"提议/讨论"拔高为"决定"，遇到模糊人名盲猜│
│                                ↓                                       │
│ 【模板呈现】单栏堆积几十行平铺条目，缺乏树状层次，用户视觉负担极重     │
└────────────────────────────────────────────────────────────────────────┘
```

### 1. 为什么内容太多太细、沦为流水账？
- **机制倒逼注水**：`length_budget.py` 中写有明文指令：`"**低于下限＝漏了关键事实**（回原文补结论/数字/责任人）"`，且在 $\ge 20\text{k}$ 原文时正文预算高达 `2400–8000` 字。LLM 为避免低于下限被判“遗漏”，必然把会上的发言过程、背景阐述、拉扯对话全都事无巨细地写入草稿。
- **归纳能力被 Prompt 锁死**：当前草稿 Prompt 要求 `"能写清就别合并"`、`"不得用一个综合结论替代多个原始事项"`、`"组内每个子事项必须以独立句或明确分项完整保留"`。这直接剥夺了 LLM 的“多发言人同类观点综合”、“过程性噪音过滤”的高级抽象总结能力，导致纪要退化为“发言速记”。

### 2. 为什么会出现“越权推断”与“盲猜人名”？
- **缺乏动词阶梯（Verb Hierarchy）**：当前 Prompt 虽然要求“讨论不升级为决策”，但缺乏可操作的动词分类规则。当会议原文中参会人说“我觉得我们下周可以先试试 A 方案”，模型容易顺手写成“会议决定下周实施 A 方案”。
- **对代称与口语转写的防猜机制不足**：ASR 语音转写常常出现“发言人1”、“那个谁”、“咱们开发人员”。模型为了满足“责任人必须是会场真实姓名”的严厉审核要求，在上下文有参会人名单时，会倾向于“挑一个最可能的名字填上去”，从而产生张冠李戴的幻觉。

### 3. 为什么视觉层级混乱、难以一目了然？
- 骨架虽然有二级标题（如 `## [工作进展]`），但栏目内部往往是 10~20 条平行并列的列表项，或者一长串独占一行的标签。用户在阅读时无法快速抓取“有哪些大议题、每个议题的核心结论是什么、有哪些次要技术参数”。

---

## 二、重构三大核心原则与规范基准

本次重构旨在确立**“高信息密度、严格真实边界、清晰树状层级”**的三大铁律：

```
                      ┌─────────────────────────┐
                      │   高质量会议纪要核心基准 │
                      └────────────┬────────────┘
                                   │
         ┌─────────────────────────┼─────────────────────────┐
         │                         │                         │
         ▼                         ▼                         ▼
┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐
│ 1. 有理有据不编造 │      │ 2. 分组分点总结   │      │ 3. 细节而不流水账 │
├──────────────────┤      ├──────────────────┤      ├──────────────────┤
│ • 结论动词阶梯   │      │ • 议题驱动（非时序│      │ • 结论与执行导向 │
│ • 零猜人名待确认 │      │ • 树状三级嵌套   │      │ • 核心参数硬锚定 │
│ • 区分事实与解读 │      │ • 同类合并归纳   │      │ • 剔除过程性废话 │
│ • 全程可溯源     │      │ • 空议题显式兜底 │      │ • 待办三要素闭环 │
└──────────────────┘      └──────────────────┘      └──────────────────┘
```

### 原则一：有理有据不编造（真实性与边界感）
1. **严格建立发言动词阶梯（Verb Ladder）**：
   - **Level 1（探讨/倾向）**：`探讨`、`倾向`、`提及`、`建议`、`交换意见`（未拍板，绝不能写成定调）。
   - **Level 2（提议/方案）**：`提议`、`汇报`、`陈述`、`承诺`、`初步排期`。
   - **Level 3（决议/拍板）**：`拍板`、`决议`、`明确要求`、`一致通过`（仅在原文有明确结论性词汇时使用）。
2. **存疑与转写模糊强制标注【待确认】**：
   - 原文人名模糊（如“那个谁”“发言者3”）、排期口径不详（如“过几天再说”）、方案存在争议的，**严禁猜测填补**，统一规范输出 `【待确认：责任人/时限/方案未明确】`。
3. **事实与解读隔离**：
   - 原文的明确数字、定调必须客观陈述；基于讨论形成的综合归纳，必须指明归属（如：“综合前端与测试意见，…”），严禁掺杂主观臆断。

### 原则二：分组分点总结（结构化与层级感）
1. **彻底打破时间流水，按议题/板块聚合**：
   - 无论发言先后顺序如何，同一议题下的所有讨论、论据、结论必须收敛在同一个小节下。
2. **推行严格的树状三级层级标准（Tree Hierarchy）**：
   - **一级（栏目）**：`## [栏名]`（由模板定义，如 `## [要点梳理]`）
   - **二级（议题/模块）**：`### 议题名称`（以会议议题为单位，禁止以人名立二级标题）
   - **三级（主干要点）**：`1. **【子议题/事项】** 进展与核心结论（责任人 ｜ 状态 ｜ 关键时限）`
   - **四级（深度支撑）**：`  - ` 缩进子条，仅用于挂载核心参数、方案对比、分歧依据或协同依赖。
3. **同类合并与发言归属**：
   - 多人就同一件事发表意见时，合并为 1 条主干结论，并在子条中归纳各方表态（如：`张三支持方案 A，李四指出成本过高，最终决定暂缓`）。
4. **空组显式兜底**：
   - 议程中预列但现场未讨论的议题，保留标题并注明 `*（该议题会上未讨论/顺延）*`，绝不凭空消失。

### 原则三：涵盖细节但不是流水账（高信息密度，有效总结）
1. **结论与成果导向（Outcome-Driven）**：
   - 砍掉一切过程性噪音：寒暄客套、主持串场、重复拉扯、思路推导的口语语气词，一律过滤。
   - 纪要只关注：**“这件事针对什么问题、定调结论是什么、依据的核心数据是什么、谁来做、何时交付”**。
2. **硬核数据与参数绝对保留（Critical Anchors）**：
   - 凡涉及金额、基线对比（如 QPS 从 500 升至 2000、WER 下降 2%）、排期日期、专有名词、技术选型，必须精准还原，不可用“大幅提升”、“显著降低”等空泛词汇替代。
3. **行动项三要素闭环**：
   - 所有待办必须满足：`[动作名称] + [唯一责任人] + [交付时间节点]`，缺一时必须显式标注待确认。

---

## 三、Minutes 全链路 Prompt 与逻辑层改进清单

为达成上述目标，需对整条流水线上的 5 个逻辑层进行系统性协同改造：

```
┌────────────────────────────────────────────────────────────────────────┐
│                        Minutes 优化协同链路矩阵                        │
├────────────────────────────────────────────────────────────────────────┤
│ 1. 预算层 (length_budget.py)  ── 砍掉40%冗余预算，废除"低于下限=漏事实" │
│ 2. 草稿 Agent (prompts.py)    ── 授权同类合并，建立动词阶梯与防猜名机制│
│ 3. 监督层 (prompts.py)        ── 保护有效归纳，专抓越权推断与盲猜人名  │
│ 4. 形态层 (body_rules.py)     ── 强制树状三级语法，规范代办三要素     │
│ 5. 模板层 (template_v3/*.md)  ── 31 个模板全面升级为两级/树状精炼结构  │
└────────────────────────────────────────────────────────────────────────┘
```

### 3.1 篇幅预算层：`length_budget.py` 降载与机制松绑

#### 问题诊断
- 当前档位为保“全量”设得过高（$\ge 20\text{k}$ 原文预算达 2400~8000 字），且 Prompt 文案直接把“低于下限”定性为“漏事实”，导致模型不敢做精炼概括。

#### 优化策略
1. **下调各档位正文预算上限 40%~50%**，为高质量概括留出合理空间：
   | 原文汉字区间 | 原 TIERS 预算 (字) | 优化后 TIERS 预算 (字) | 目标定位 |
   |---|---|---|---|
   | < 3k | 720 – 1800 | **500 – 1000** | 极简提炼，突出结论 |
   | 3k – 8k | 1080 – 3200 | **800 – 1600** | 议题清晰，核心参数齐备 |
   | 8k – 20k | 1680 – 5500 | **1200 – 2400** | 树状分层，去粗取精 |
   | $\ge$ 20k | 2400 – 8000 | **1800 – 3200** | 深度综合，严禁流水账 |
2. **彻底重构 `budget_line()` 提示词文案**：
   - ❌ **删除**：`"**低于下限＝漏了关键事实**（回原文补结论/数字/责任人，不扩写寒暄与过程铺陈）"`
   - ✅ **替换为**：`"【篇幅指引】在完整覆盖核心议题与决议的前提下，力求言简意赅、高信息密度。达到下限即说明信息覆盖充分；杜绝为了追求篇幅而罗列发言过程流水账；优先使用紧凑的树状结构组织事实。"`
3. **保留物理硬熔断**：保留 `output_token_cap()`，维持防死循环与防显存溢出的安全底线。

---

### 3.2 草稿 Agent：`MINUTES_GENERATION_SYSTEM_PROMPT` 归纳授权与动词阶梯

#### 优化策略清单
1. **解除“反压缩”紧箍咒，正式授权同类合并**：
   - 修改前：`“能写清就别合并；不得用一个综合结论替代多个原始事项”`
   - 修改后：`“【归纳与合并授权】对同一议题下多人的讨论、辩驳或同类技术进展，必须进行提炼合并，形成结构化的议题总结。严禁按发言时间顺序流水账式记录谁说了什么；单项事实交代清‘结论 + 关键数字/参数 + 责任归属’即可，彻底剔除寒暄、跑题与口语废话。”`
2. **注入“动词阶梯与真实性边界纪律”**：
   - 增加明文约束：
     ```markdown
     【真实性边界与动词阶梯】
     1. 严禁越权推断：原文仅讨论/提议（如“可以试试”、“我建议”）的内容，动词必须使用“探讨/建议”，严禁升级为“决定/拍板/一致同意”。
     2. 零盲猜人名：遇到语音转写不清或称呼代词（如“那个谁”、“发言人1”），严禁从参会人名单中主观臆测人名填入；无法确认身份的统一标注为“【待确认】”或采用客观无主语句式。
     3. 存疑标注：凡是排期未定、指标模糊或存在争议未形成共识的，明确标注【待确认：xxx】。
     ```
3. **提炼字段 `executive_summary` 契约升级**：
   - 从“成段长篇大论”转向“核心议题概貌”：每段严格围绕一个核心议题，2~3 句概括背景与最终定调，不再塞入过细的分支参数（分支参数全部下沉至明细要点）。
4. **搬运类与提炼类字段清晰解耦**：
   - `key_decisions`：必须对应 Level 3 拍板决议，无明确决议输出 `[]`。
   - `personally_relevant_points`：严格要求“动作 + 责任人 + 时限”三要素闭环，未提及要素直接缺项，严禁杜撰。

---

### 3.3 监督审查层：`MINUTES_SUPERVISOR_DOMAIN_PROMPT` 边界卡控与极简留痕

#### 优化策略清单
1. **调整拦截阈值，保护有效归纳**：
   - 明确规定：**“提炼字段中对同类事实的高质量合并与抽象归纳，只要保留了核心结论与关键参数，一律视为优秀输出，严禁以‘丢失发言过程’或‘条数减少’为由判定为遗漏。”**
   - 废除“草稿字数不足下限 60% 则拦截”的机械规则，改为“仅当丢失核心议题或关键拍板决策时才拦截”。
2. **强化三大红线拦截（专抓致命硬伤）**：
   - **拦截点 1（越权推断）**：将讨论/探讨/分歧强行定性为“达成一致/会议决定”；
   - **拦截点 2（盲猜代称）**：把模糊代词或无支撑角色擅自脑补为主观指定的人名；
   - **拦截点 3（流水账噪音）**：在提炼字段中大段复述无意义的发言对话过程。
3. **维持极简留痕纪律**：
   - findings 严禁长篇大论，仅限输出 1~3 条核心缺陷，每条 $\le 100$ 字，格式为 `[位置] + [缺陷类型] + [原文依据]`，无缺陷必须输出 `[]`。

---

### 3.4 正文形态与渲染层：`body_rules.py` 与 `template_prompt.py` 树状层级规范

#### 优化策略清单
1. **在 `BODY_FORMAT_RULES` 中推行全域“树状三级标准（Tree Hierarchy）”**：
   - 规定正文默认形态必须为树状结构，杜绝平铺清单：
     ```markdown
     ### [议题/模块名称]
     1. **【子议题/核心事项】** 进展与核心结论（责任人 ｜ 状态 ｜ 关键时限）
        - 核心参数与量化依据（如：QPS 提升至 3500，延时 < 20ms）
        - 关键分歧或前提依赖（如：李四对成本提出异议，依赖上游数据同步）
     ```
2. **待办事项格式标准化（严格三要素）**：
   - 统一采用交互复选框：`- [ ] **事项名称**（责任人 ｜ 完成时限 ｜ 验收标准）`。
   - 严禁缺失关键要素：原文没有时限写 `【待确认：时限】`，绝不凭空猜测。
3. **过滤流水账句式规则**：
   - 明文禁止：“首先…接着…随后…然后…”、“张三发表了关于…的看法，李四随后补充了…”。
   - 必须转化为结果导向的客观综合陈述。

---

## 四、template_v3 模板库分级优化方案

当前 `template_v3/` 下共有 31 个 Markdown 模板。我们将模板划分为**基石通用类、个人视角类、以及 4 大专业场景分类**进行针对性优化：

```
                      ┌────────────────────────────┐
                      │    template_v3 模板库分类   │
                      └─────────────┬──────────────┘
                                    │
    ┌────────────────┬──────────────┴───────────────┬────────────────┐
    │                │                              │                │
    ▼                ▼                              ▼                ▼
【通用基石】      【个人视角】                    【研发/项目类】  【评审/决策类】
general_minutes   personal_minutes               team_meeting     decision_review
                                                 project_progress contract_vetting
                                                 retrospective... hiring_report...
                                                        │                │
                                      ┌─────────────────┴────────────────┘
                                      ▼                                  ▼
                              【宣讲/访谈/研讨类】              【严肃专业记录类】
                              media_briefing, interview...      court, legal, clinical...
```

---

### 4.1 基石模板：`general_minutes.md`（通用纪要）重构设计

#### 重构目标
通用纪要已重构为“2 栏式”（`全文摘要` + `要点梳理`）。本次需深化其内部结构，彻底解决“要点梳理变成大段流水账”的问题，树立全库标杆。

#### 优化后模板结构定义
```markdown
# 通用纪要

<!-- requirement
客观呈现会议全貌与核心决议，语气客观中立、去流水账化；严格遵循发言动词阶梯，严禁将探讨推断为决议，严禁盲猜人名；采用树状三级结构组织要点，兼顾高信息密度与一目了然的层级感。
-->

## [全文摘要]
[一段话（约 200–300 字）全局高度概括：① 会议场合、核心议题主线与参会主体；② 最终达成的一致决议与定调基调（未形成决议则写明「以信息同步与探讨为主，未形成明确决议」）；③ 提炼 2–4 个全场最核心的量化指标、关键日期或里程碑作为锚点；本栏只讲全局定调，严禁展开具体分项讨论细节，明细全部沉淀在 [要点梳理]]

## [要点梳理]
[按会议核心议题全面展开，采用清晰的树状分层结构，严禁按时间顺序记流水账；分为【议题树状梳理】与末尾【固定三组件】：

一、议题树状梳理（覆盖会议所有讨论板块，一个议题一个小节）：
- 每个议题一行小节标题 `### 议题名称`（若议题未讨论，保留标题并注明 `*（该议题会上无讨论/顺延）*`）。
- 议题下方最顶层级按数字序号（1. 2. 3. ...）列出主干事实，格式为：
  `1. **【子议题/事项】** 进展与核心结论（责任人 ｜ 状态 ｜ 关键参数/交付物）`
- 次级细节必须下挂缩进子条 `  - ` 展开，仅保留：
  - 核心量化指标与参数对比（技术基线、金额、比例）；
  - 多方分歧与关键异议（如：`张三支持方案 A，李四指出性能瓶颈，经讨论决定采用折中方案 B`）；
  - 前提依赖与上下游制约。
  - 彻底剔除无意义的辩驳过程与寒暄，保留论点与论据。

二、末尾固定三组件（有内容才出现，一条只写一件事）：
- `### 决议与定调`：按数字序号（1. 2. 3. ...）列出拍板或一致达成的硬核结论，交代决策依据与适用口径；无明确拍板则不出现该节。
- `### 行动项与分工`：统一采用交互复选框清单 `- [ ] ` 一行一项列出：
  `- [ ] **待办事项**（责任人 ｜ 完成时限 ｜ 验收标准/成果）`
  必须包含动作、责任人与时限三要素，原文未提时限标注【待确认：时限】；严禁无主待办。
- `### 待确认与风险`：按数字序号或 `- ` 列出，标注【高风险】、【阻塞】或【待确认】，交代症结卡点与跟进人。]
```

---

### 4.2 核心模板：`personal_minutes.md`（个人视角纪要）重构设计

#### 重构目标
沉浸式工作台体验，强化与“本人”的高度关联，严格遵守“本人动作省主语、他人动作写真名”，同时避免他人无关发言的流水账干扰。

#### 优化后模板结构定义
```markdown
# 个人视角纪要

<!-- requirement
以本人工作台视角提炼会议输入，突出"与我相关"的定调、待办与协同依赖；严守本人动作省主语、他人动作写真名；紧扣 focus_thing（重点事项）与 focus_person（关注人物），过滤与本人无关的琐碎讨论；严禁使用任何 emoji，严禁机械填充占位符。
-->

## [本场概况与本人定调]
[一段话（约 200–280 字）兼顾全局态势与个人定位：① 前半句交代会议核心议程与全局关键决议；② 紧接聚焦本人角色与 focus_thing，明确会议对本人负责模块提出的核心定调、目标指标与排期红线；体现 focus_person 的关键指示；③ 若本场无本人直接事项，写明「本场无本人直接牵头事项，重点关注相关协同与指标进展」]

## [重点关注与业务进展]
[按数字序号（1. 2. 3. ...）一行一项列出，紧扣 focus_thing 与 focus_person 展开，条目格式为：
`1. **【关注事项/模块名】进展与定调**（交付节点 ｜ 核心指标）`
- 下挂缩进子条 `  - ` 交代方案决策、核心参数对比与上下游制约；
- 成句交代 focus_person 的关键技术判断与承诺（写真名）；
- 涉及本人动作严格省主语；他人主导但对本人有制约的事项一句话交代要点，纯属他人事务坚决过滤；一条只写一件事]

## [行动项与协同依赖]
[分层工作台看板呈现，一条一件事：
① ### 与我相关待办（或独占一行 `**与我相关**：`）：最顶层级一律采用交互复选框 `- [ ] ` 一行一项列出：
`- [ ] **任务名称**（交付时限 ｜ 核心交付物 ｜ 验收标准）`
条目内严禁出现本人姓名或“我/你”，直接以动词开头（如：`- [ ] 完成网关接口重构（9月30日前 ｜ PR已合并 ｜ 通过压力测试）`）；
② ### 协同输入与依赖（或独占一行 `**前置依赖**：`）：列出推进本人工作所必须的外部交付：
`- **【协同输入】姓名**：具体交付物/接口/资源承诺（交付时限）`，写清“谁提供、何时交付、依赖此项开展什么”；
③ ### 协同人员主要分工（或按人独占一行 `**姓名**：`）：仅保留与本人业务有交集的他人待办，无关人员分工坚决不列]

## [待确认事项与风险卡点]
[分两块呈现，条目独立成行：
① ### 本人及关注事项卡点（或独占一行 `**与我相关**：`）：按复选框 `- [ ] ` 列出直接阻塞本人进度的卡点，重要阻塞标注【阻塞】、潜在风险标注【高风险】，写清跟进人与症结；
② ### 全局重大风险与未决争议：仅列出制约整体上线周期的全局性技术风险与未决分歧，没有则不出现该小节]
```

---

### 4.3 专业场景模板（29 个）：场景化分类与标准化对齐清单

对其余 29 个专业模板，统一贯彻“树状层级化、动词阶梯化、三要素闭环化、去流水账化”的原则进行针对性对齐：

#### 类别一：研发与项目管理类（6 个）
> 模板：`team_meeting.md`（团队例会）、`project_progress.md`（项目进度）、`retrospective_session.md`（复盘会）、`workshop_session.md`（工作坊）、`product_launch.md`（产品发布）、`admission_briefing.md`（筹备宣讲）

- **共性改造点**：
  1. 杜绝按发言人流水账汇报，改为按“模块/业务线”设立 `### 模块名`；
  2. 规范【阻塞】与【延期】的判定口径，必须写清卡点症结与阻碍天数；
  3. `[决定与待办]` / `[后续计划]` 统一强制要求 `- [ ] **事项**（责任人 ｜ 时限 ｜ 验收标准）` 复选框三要素；
  4. 协作依赖明确采用流向符 `发起方 ➔ 配合方`。

#### 类别二：评审与决策审核类（5 个）
> 模板：`decision_review.md`（决策评审）、`contract_vetting.md`（合同审查）、`hiring_report.md`（招聘面试复盘）、`group_seminar.md`（课题评审）、`debate_forum.md`（辩论研讨）

- **共性改造点**：
  1. 核心论点结构化：采用表格或对齐卡片，对比“方案陈述 vs. 评审异议 vs. 支撑数据”；
  2. 决策结论严格遵循动词阶梯：区分“无条件通过”、“有条件通过（附加整改项）”、“否决驳回”；
  3. 遗留问题强制挂载“整改责任人 + 复审节点”。

#### 类别三：沟通、访谈与研讨类（10 个）
> 模板：`media_briefing.md`（媒体通气）、`media_qa_session.md`（媒体问答）、`interview_debrief.md`（访谈复盘）、`interview_transcript.md`（访谈实录）、`exchange_forum.md`（交流研讨）、`research_dialogue.md`（研究对话）、`home_school_liaison.md`（家校沟通）、`conversation_transcript.md`（对话实录）、`knowledge_memo.md`（知识备忘）、`personal_memo.md`（个人备忘）

- **共性改造点**：
  1. 问答提取真问答：合并同一方的连续追问，答复仅保留核心要点，过滤客套与铺垫；
  2. 观点归纳提炼：严禁“你说一句、我答一句”的琐碎实录（除 `transcript` 笔录类模板外），提炼核心见解与共识；
  3. 归属严谨性：明确提问方机构/角色与受访人姓名，不盲猜身份。

#### 类别四：严肃专业与咨询记录类（8 个）
> 模板：`court_transcript.md`（法庭审理笔录）、`legal_advisory.md`（法律咨询）、`clinical_advisory.md`（临床咨询会诊）、`psychological_session.md`（心理咨询会话）、`government_bulletin.md`（政府公报）、`special_lecture.md`（专题讲座）、`class_transcript.md`（课堂纪要）、`site_visit_tour.md`（实地调研参观）

- **共性改造点**：
  1. 零推断、字字有据：法律、医疗与庭审记录绝对禁止任何主观归纳或演绎，保留原文严谨限定词（如“原则上”、“暂定”）；
  2. 存疑标注：对药名、金额、法律条款条目编号不确定的，必须标注 `【转写存疑/待核对】`，严禁猜测；
  3. 结构保留：严格保留专业模板特定的事实要素与逻辑结构。

---

## 五、重构效果对比示范（Before vs. After）

通过具体场景案例，直观展示改进前后的巨大差异：

### 案例 1：讨论过程 vs. 结果导向的高密度提炼
*【会议原文片段】*：“张工说我们现在的缓存方案 QPS 只能跑到 1500，容易把数据库打死，建议换成 Redis 集群方案。李工说换集群方案成本太高，运维人力也不够，能不能先优化 SQL。两人争执了大概十分钟，最后王总拍板说，下周由张工先搞一个集群方案的 POC 测试，如果下周五前 QPS 能过 3500 且延时低于 15ms，就按张工的办，否则还是走 SQL 优化。”

| 维度 | 现状（流水账、字数多、低密度） | 改进后（树状、结论导向、高密度） |
|---|---|---|
| **呈现形式** | 大段叙述双方争执过程，先后列出张工和李工的发话 | 树状归组，先结论后依据，砍掉争端铺垫 |
| **具体内容** | 会上张工指出当前缓存方案 QPS 仅 1500，存在打垮数据库风险，提出更换为 Redis 集群。李工对此提出异议，认为成本与运维负担过重，主张优先优化 SQL。双方进行了深入讨论分析。最终王总决定先进行测试。 | **### 数据库缓存架构改造**<br>`1. **【缓存架构】** 启动 Redis 集群方案验证，限期完成可行性判定（张工 ｜ 进行中 ｜ 下周五前交付 POC 报告）`<br>&nbsp;&nbsp;`- 性能达标基线：QPS ≥ 3500 且 延时 < 15ms；未达标则回退为 SQL 优化路线`<br>&nbsp;&nbsp;`- 各方考量：李工曾对集群运维成本与人力提出异议，暂以 POC 实测结果为决策依据` |
| **分析** | 128 字，全是“张工说、李工说、争执十分钟”的过程废话，核心验收标准模糊。 | **82 字**，层级分明，结论、责任人、时限、验收标准、分歧焦点一目了然。 |

---

### 案例 2：动词越权与盲猜 vs. 边界分明与存疑标注
*【会议原文片段】*：“那个谁……就是新来的那个前端负责人，提了一句说微前端架构可能对加载速度有帮助，大家觉得下个季度是不是可以调研一下。散会前也没定具体谁做。”

| 维度 | 现状（越权推断、盲猜人名） | 改进后（动词阶梯、存疑兜底） |
|---|---|---|
| **处理方式** | 模型从名单中猜了新员工“孙某”，并将提议定性为会议决定 | 动词定性为“探讨/建议”，责任人标注【待确认】 |
| **具体内容** | `### 架构决策`<br>`- 会议决定由前端负责人孙某在下季度全面推行微前端架构调研，以优化加载速度。` | `### 架构演进探讨`<br>`1. **【微前端】** 探讨引入微前端优化加载速度的可行性（建议阶段，未形成决议）`<br>&nbsp;&nbsp;`- 待办状态：【待确认：责任人未指定 / 调研排期待明确】` |
| **分析** | 严重违背事实！把随口提议推断为会议决定，擅自安给人名孙某，极度危险。 | **真实可靠！** 准确使用“探讨”动词，客观标注存疑，零幻觉。 |

---

## 六、分阶段落地实施路线图与测试验收方案

为保证系统稳定过渡、无破坏性变更，建议按如下 4 个阶段逐步落地实施：

```
Phase 1: 预算层与正文规则库改造 (length_budget.py + body_rules.py)
  │
  ▼
Phase 2: 草稿与监督 Prompt 优化 (domain/meeting/tasks/minutes/prompts.py)
  │
  ▼
Phase 3: 通用与个人基石模板重构 (general_minutes.md + personal_minutes.md)
  │
  ▼
Phase 4: 29 个专业场景模板批量标准化 + 全量回归测试验收
```

### 实施路线步骤清单

#### 阶段一：基础支撑层与规则库改造（无风险先行）
1. **修改 `tools/templates/length_budget.py`**：
   - 调整 `TIERS` 预算区间（下调 40%~50%）；
   - 更新 `budget_line()` 文案，废止“低于下限＝漏事实”；
   - 保持单元测试对有效预算计算的兼容。
2. **修改 `tools/templates/body_rules.py`**：
   - 将树状三级标准（`### 议题` $\to$ `1. **【子项】**` $\to$ `  - `）写入 `BODY_FORMAT_RULES`；
   - 明确待办项复选框三要素规范与反流水账规则。

#### 阶段二：Prompt 契约与监督层重构
1. **修改 `domain/meeting/tasks/minutes/prompts.py`**：
   - `MINUTES_GENERATION_SYSTEM_PROMPT`：注入动词阶梯表、授权同类合并归纳、注入转写防盲猜机制；
   - `MINUTES_SUPERVISOR_DOMAIN_PROMPT`：保护合理归纳，建立越权推断与盲猜人名的高优先级拦截，保持 findings 极简留痕；
   - `MINUTES_RENDER_PROMPT`：强化树状排版渲染逻辑。

#### 阶段三：基石与核心模板重构
1. **重构 `template_v3/general_minutes.md`**：应用树状分层结构与末尾三组件，确立全库规范。
2. **重构 `template_v3/personal_minutes.md`**：应用工作台视图、省主语及协同依赖看板。
3. **针对性回归测试**：运行 `tests/test_template_router.py` 与 minutes 专项测试，确保占位符解析与字段对齐 100% 正常。

#### 阶段四：专业模板全面对齐与全量回归验收
1. **批量更新 `template_v3/` 下其余 29 个模板**：
   - 注入议题树状结构、复选框三要素待办看板与【待确认】规范。
2. **全量回归验证标准**：
   - 运行 `pytest tests/`，保证原有所有测试用例 100% 通过（当前基线 129/129 通过）；
   - 执行长文本真实用例基准测试，统计指标：
     - 正文字数压缩率：平均字数下降 30%~45%；
     - 生成总耗时：下降 25%~35%；
     - 幻觉与越权率：越权推断与猜名率为 0；
     - 用户层级满意度：树状层次清晰，一目了然。

---

## 七、总结与确认

本改进策略清单从**底层预算、生成逻辑、审查边界、形态规范到 31 个场景模板**，完成了和谐统一、逻辑闭环的顶层设计。  
它既彻底解放了 LLM 的归纳与高密度总结能力，消除了流水账和篇幅膨胀，又设立了铁一般的动词阶梯与存疑边界，牢牢守住了“真实不编造”的底线。

> **请审阅本方案**。待您确认满意后，我们将按阶段逐步推进代码与模板落地！
