# 编排内核
langgraph>=0.6.0,<2

# FastAPI 后端接口（app/ 包；启动 uvicorn app.main:app）
fastapi>=0.110
uvicorn>=0.29
redis>=5.0

# 学科目录拼音化（catalog 按 subject 分目录：数学 → shuxue）
pypinyin>=0.5

# WebSocket LLM 后端（tools/llm/llmclient.py；LLM_BACKEND=websocket 时需要）
websockets>=12.0

# 可选：思维导图 PNG 导出（markmap HTML 截图）需要浏览器内核。
# 安装 Python 包后还需执行：python -m playwright install chromium
# 未安装时导图导出自动降级（仅 HTML 或跳过 PNG），不影响其他功能。
playwright>=1.40

# 知识库（tools.knowledge）：PPT/PDF/docx/xlsx 入库 + 向量检索 + 出处
chromadb>=0.5.0
openai>=1.0.0
pdfplumber>=0.11.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0

# 自测（tests/）：零 LLM 断言套件，运行 python -m tests（自带 runner，不依赖 pytest）
pytest>=7.0

# 系统依赖不由 pip 安装：
# - 知识图谱 HTML：使用 Cytoscape.js CDN，联网打开效果最佳。
# - 思维导图 HTML：需要 Node.js/npx，npx 会按需拉取 markmap-cli。
