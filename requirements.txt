# Agent workflow orchestration
langgraph==1.2.10

# Web testing / demo UI
# The current gradio_app.py uses the Gradio 6 launch API.
gradio>=6.0
websockets>=12.0

# 可选依赖：思维导图 PNG 导出（markmap HTML 截图）需要浏览器内核。
# 安装 Python 包后，还需要执行：python -m playwright install chromium
# 未安装时 --task mindmap 自动降级（仅 HTML 或跳过 PNG），不影响其他功能。
playwright>=1.40

# 系统依赖不由 pip 安装：
# - 知识图谱 PNG/SVG：需要 Graphviz dot 命令
#   Ubuntu/Debian: sudo apt-get install -y graphviz fonts-noto-cjk
#   CentOS/RHEL:   sudo dnf install -y graphviz google-noto-sans-cjk-fonts
# - 知识图谱 HTML：使用 Cytoscape.js CDN，联网打开效果最佳；离线请使用同目录 SVG。
# - 思维导图 HTML：需要 Node.js/npx，npx 会按需拉取 markmap-cli。
