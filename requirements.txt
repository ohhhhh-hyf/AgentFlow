# 编排内核
langgraph>=0.6.0,<2

# Web 测试 / 演示 UI（gradio_app.py；兼容 Gradio 4.x-5.x API）
gradio>=4.44.0,<6

# WebSocket LLM 后端（llm_client；LLM_BACKEND=websocket 时需要）
websockets>=12.0

# 可选：思维导图 PNG 导出（markmap HTML 截图）需要浏览器内核。
# 安装 Python 包后还需执行：python -m playwright install chromium
# 未安装时 --task mindmap 自动降级（仅 HTML 或跳过 PNG），不影响其他功能。
playwright>=1.40

# 知识库（tools.knowledge）：PPT/PDF/docx/xlsx 入库 + 向量检索 + 出处
chromadb>=0.5.0
openai>=1.0.0
pdfplumber>=0.11.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0

# 测试（tests/）：纯函数单测 + LLM 桩测 + 全库 Python 3.10 语法兼容检查
pytest>=7.0
pytest-asyncio>=0.23

# 系统依赖不由 pip 安装：
# - 知识图谱 PNG/SVG：需要 Graphviz `dot` 命令
#   Ubuntu/Debian: sudo apt-get install -y graphviz fonts-noto-cjk
#   CentOS/RHEL:   sudo dnf install -y graphviz google-noto-sans-cjk-fonts
# - 知识图谱 HTML：使用 Cytoscape.js CDN，联网打开效果最佳；离线请使用同目录 SVG。
# - 思维导图 HTML：需要 Node.js/npx，npx 会按需拉取 markmap-cli。
