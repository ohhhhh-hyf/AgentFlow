"""零 LLM 自测套件：全部为确定性断言，秒级、不花钱、可进 CI。

设计口径
--------
- **只测"程序说了算"的部分**：契约不变量、硬执行规则、提示词里必须存在的纪律、
  画像选档与合并、记忆状态机、模板解析与门禁方向。凡是需要模型判断的，这里一律不测
  （所以每个套件都叫"零 LLM"）。提示词断言是刻意的：它们钉的是
  "某条纪律必须出现在发给模型的文本里"，避免有人把纪律误删或改漂。
- **一个套件一个文件**，文件名 = 被测子系统名；每套自带 ``check()`` 与 ``main()``：
  打印 ``pass N fail M``，失败退出码 1（全绿 0）。
- 运行方式（任选）：

      python -m tests                      # 全部套件，汇总一行结果
      python -m tests.test_template_router  # 单个套件（推荐定位问题时用）

  位置沿革：套件原与源码同目录（``tools/core/_selftest.py`` 等）→ 2026-09-21 收拢到
  ``tools/test/`` → 2026-09-22 迁到顶层 ``tests/``（与源码分离，且不叫 ``test``：
  避免遮蔽 Python 标准库的 ``test`` 包）。每次搬迁只改导入路径与运行命令，断言未变。
"""
