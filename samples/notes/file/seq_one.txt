# 函数与单调性

函数是两个数集之间的对应：每个自变量只对应一个函数值。定义域要同时满足分母不为零、偶次根号下非负、对数真数大于零，取这些条件的交集。

单调性用定义法证明时，按「取值、作差、变形、定号、下结论」五步走。作差后常用因式分解或通分来判断符号。后面会用导数来加快判断：导数大于零则递增，导数小于零则递减，这一条先记下，下一份笔记展开。

奇偶性判断之前必须先看定义域是否关于原点对称。不对称就既不是奇函数也不是偶函数。
