# 纪要任务执行轨迹文档

> 对应命令：`python bootstrap.py --task minutes --summary ... --profile ...`
> 说明：本文档用 FakeClient 逐步调用 orchestrator 的真实节点方法，展示每个阶段**做了什么** + **state 变成了什么**。待办线（action_items）流程同构，仅草稿/审核模型与渲染 prompt 不同。

## 输入

```
会议文本: "项目中期验收会议纪要\n\n会议于周六上午召开。王明汇报了项目整体进度，
          目前项目完成度约 80%。李明提出前端页面还有 3 个 bug 需要修复。"
用户: {"name": "李明", "perspective": "personal", ...}   ← 个人视角
```

---

## 第 0 步：bootstrap 预处理（`_normalize_transcript`）

**做什么**：归一化会议文本——合并段落内硬换行、保留段落间空行（处理 PDF 复制/OCR 场景）。

**state 变化**：`transcript` 变成归一化后的字符串（本例无脏格式，原样保留）。

```python
state = {
    "transcript": "项目中期验收会议纪要\n\n会议于周六上午召开。...3 个 bug 需要修复。",
    "user": {"name": "李明", "perspective": "personal", ...},
    "objective_perspective": false,      # ← 个人视角 → False
    "meeting_understanding": null,       # 空（等待 core 填充）
    "perspective_profile": null,
    "lines": {},                          # 空（等待各线写入）
    "quality_degraded": false,
}
```

---

## 第 1 步：core 会议理解节点（`_meeting_understanding_node`）

**做什么**：把 `transcript + user` 喂给 LLM，提取**客观会议理解**（`MeetingUnderstanding` 模型）：会议目的/议题/决策/未决问题/风险。

**state 变化**：`meeting_understanding` 从 `null` → 结构化 dict：

```json
"meeting_understanding": {
  "meeting_purpose": "项目中期验收",
  "topics": ["整体进度", "前端 bug"],
  "decisions": ["验收定于下周五上午"],
  "open_questions": ["测试环境是否可用"],
  "risks": ["bug 影响验收"]
}
```

---

## 第 2 步：core 视角建模节点（`_perspective_modeling_node`）

**做什么**：结合用户画像 + 会议理解，LLM 建**用户视角模型**（`PerspectiveModeling`）：该用户关心的议题/职责/顾虑——决定"这份纪要往谁的方向写"。

**state 变化**：`perspective_profile` 从 `null` → dict：

```json
"perspective_profile": {
  "name": "李明", "inferred_role": "前端负责人",
  "responsibilities": ["修复前端 bug"], "goals": ["完成验收"],
  "concerns": ["时间紧张"], "relevant_topics": ["前端 bug"],
  "evidence": ["会议中李明提出"]
}
```

---

## 第 3 步：共享上下文拼装（`_shared_context`）

**做什么**：把 `视角模式 + 用户画像 + 会议理解 + 视角模型` 拼成一段文本——**agent/supervisor 都用它**（不含会议原文全文，那是渲染时才给）。

**state 变化**：无（纯文本生成，不写 state）。

```
视角模式：personal
用户画像：{...}
会议理解：{...}
用户视角模型：{...}
```

---

## 第 4 步：纪要线 agent 节点（生成草稿）

**做什么**：`_make_agent_node("minutes_generation")` → 用共享上下文让 LLM 生成 `Minutes` 草稿（headline + 6 个结构化字段），`Minutes.validate()` 校验后存入。

**state 变化**：`lines` 从 `{}` → 出现第一个抽屉：

```json
"lines": {
  "minutes_generation": {
    "draft": {
      "headline": "项目中期验收会",
      "executive_summary": ["完成度 80%", "前端 3 个 bug"],
      "key_decisions": ["验收定于下周五"],
      "personally_relevant_points": ["修复前端 bug"],
      "risks_and_blockers": ["bug 影响验收"],
      "unresolved_questions": ["测试环境"]
    }
  }
}
```

> draft 的 6 个字段来自 `MinutesGenerationContract.fields`（契约驱动）。

---

## 第 5 步：纪要线 supervisor 节点（审核草稿）

**做什么**：`_make_supervisor_node("minutes_generation")` → 用"全局标准 + 纪要领域规则"审核草稿，`MinutesSupervisorReview.validate()` 校验后返回 `approve / revise / reject`。

**state 变化**：`lines["minutes_generation"]` **字段级合并**（`_merge_lines` reducer）——draft 保留，新增两键：

```json
"lines": {
  "minutes_generation": {
    "draft": { ... },                    # 保留
    "supervisor_review": {
      "decision": "approve",             # 通过 → 走渲染；revise → 返工；reject → 降级
      "facts_check": {"status": "pass", "findings": []},
      "perspective_check": {"status": "pass", "findings": []},
      "consistency_check": {"status": "pass", "findings": []},
      "feedback": []
    },
    "revision_feedback": []
  }
}
```

> 审核维度来自 `MinutesSupervisorContract.checks`（3 个 Check）。路由：`approve` → 渲染；`revise` → 返工闭环（≤ 上限）；`reject`/超限 → 降级。

---

## 第 6 步：渲染（`_produce`）

**做什么**：
1. `_minutes_generation_render_context(state)` 拼渲染上下文（**此时才给会议原文全文** + 已审核的 draft/审核结论）
2. 喂给 LLM（`MINUTES_RENDER_PROMPT`）流式渲染
3. 流式尾部写回完整文本

**渲染上下文**（喂给渲染 LLM 的）：

```
视角模式：personal
objective_perspective：False
会议原文：<全文>
用户画像：{...}
已审核会议理解：{...}
已审核用户视角：{...}
已批准纪要草稿：{...}
纪要审核结论：{approve, ...}
```

**state 变化**：`lines["minutes_generation"]` 新增 `rendered`：

```json
"lines": {
  "minutes_generation": {
    "draft": { ... },
    "supervisor_review": { "decision": "approve", ... },
    "revision_feedback": [],
    "rendered": "项目完成度约 80%。会议确定中期验收定于下周五上午，
                 前端 3 个 bug 由李明负责在验收前修复。"
  }
}
```

---

## 第 7 步：最终 Report 组装（`_assemble_report`）

**做什么**：遍历 `MinutesReport` 字段，按 `metadata["source"]` 标签取值：
- `title` ← `_compute_title`（个人视角 → "李明视角会议纪要"）
- `rendered` ← `lines["minutes_generation"]["rendered"]`
- `quality_warning` ← 系统填（正常 = null）

**最终交付**（`done.reports["minutes_generation"]`）：

```json
{
  "title": "李明视角会议纪要",
  "personalized_minutes": "项目完成度约 80%。会议确定中期验收定于下周五上午，
                           前端 3 个 bug 由李明负责在验收前修复。",
  "quality_warning": null
}
```

---

## 全程 state 演变速览

```
transcript + user + objective_perspective(false) + lines:{}         ← 第 0 步
  │ 第 1 步  meeting_understanding ← 客观会议理解
  │ 第 2 步  perspective_profile   ← 用户视角模型
  │ 第 3 步  （shared_context 拼文本，不写 state）
  │ 第 4 步  lines["minutes_generation"].draft ← LLM 草稿（6 字段）
  │ 第 5 步  + supervisor_review（approve）+ revision_feedback   ← 字段级合并
  │ 第 6 步  + rendered（渲染正文）
  ▼ 第 7 步  _assemble_report → done.reports["minutes_generation"]
              = {title: "李明视角会议纪要", personalized_minutes: ..., quality_warning: null}
```

---

## 关键机制点

1. **`_merge_lines` 字段级合并**：第 4→5→6 步三次写同一抽屉（draft → review → rendered），reducer 逐个合并，**不覆盖已有键**（第 5 步后 draft 仍在）。
2. **会议原文只在渲染时给**（第 6 步上下文）——agent/supervisor 用"会议理解"（提炼结果），渲染才看原文（核对措辞）。
3. **`approve` 路由**决定走渲染（本例）还是返工/降级——`quality_warning` 只有降级时才有值。
4. **契约驱动**：draft 的 6 个字段来自 `MinutesGenerationContract.fields`，supervisor_review 的 3 个 check 来自 `MinutesSupervisorContract.checks`——改契约，这里自动变。
5. **单任务模式**：一次只跑一条线（bootstrap 的 `--task` 为单值），`lines` 里只出现所选线的抽屉。
