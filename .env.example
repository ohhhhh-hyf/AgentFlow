# 复制本文件为 .env 并填写你的真实 Key：
#   Windows: copy .env.example .env
#   Linux/macOS: cp .env.example .env

# ── 必填：DeepSeek API Key（在 https://platform.deepseek.com 申请）──
DEEPSEEK_API_KEY=your_api_key_here

# ── 可选：DeepSeek 覆盖项（有默认值，不配置也能运行）──
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_TEMPERATURE=0

# ── 可选：输入/模板路径配置（命令行显式参数优先于这些配置）──
# 不配置时默认使用领域包内样例：
#   MEETING_SUMMARY          = src/domain/meeting/samples/summary
#   MEETING_PROFILE          = src/domain/meeting/samples/profile
#   MEETING_SUMMARY_TEMPLATE = 未配置（使用默认自由段落格式）
#   MEETING_ITEM_TEMPLATE    = 未配置（使用默认列表格式）
#
# 配置示例（配好后可直接 `python bootstrap.py` 零参数运行）：
# MEETING_SUMMARY=src/domain/meeting/samples/summary/meeting.txt
# MEETING_PROFILE=src/domain/meeting/samples/profile/user_profile.json
# MEETING_SUMMARY_TEMPLATE=src/domain/meeting/samples/summary_template/project_progress.md
# MEETING_ITEM_TEMPLATE=src/domain/meeting/samples/item_template/action_items.md
