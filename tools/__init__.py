"""tools —— 通用工具（与领域无关，供任意业务复用）。

子模块（tools.validation / tools.contracts / tools.fallback_rules /
tools.prompt_utils / tools.template_prompt / tools.logging_config）
一律以 ``from tools.xxx import ...`` 方式使用，本包不提供顶层转发导出。
"""
