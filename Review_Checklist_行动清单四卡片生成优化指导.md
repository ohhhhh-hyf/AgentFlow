# Review Checklist 行动清单四卡片生成优化指导

## 一、目标

本次优化 **只针对 Review Checklist 的“行动清单”模块**。

现有行动清单已经能够正确生成四部分内容：

1. 基础快速过关
2. 核心冲刺
3. 重点专项突破
4. 扫雷补漏

下一步不要继续输出为一整段连续 Markdown，而是将四部分整理成 **4 张独立卡片的数据内容**。

前端交互方式：

```text
4 张阶段卡片
      ↓
点击某张卡片
      ↓
展示该卡片对应的详细内容
```

AI 只负责生成卡片所需的结构化文字内容。

AI 不负责：

- HTML
- CSS
- 点击事件
- Tab 切换
- 动画
- 进度状态

---

# 二、最终固定生成四张卡片

卡片数量固定为 4 张：

```text
card_1  基础快速过关
card_2  核心冲刺
card_3  重点专项突破
card_4  扫雷补漏
```

不要因为某部分内容较少就新增、删除或拆分顶层卡片。

四张卡片用于表达完整复习路线：

```text
基础快速过关
      ↓
核心冲刺
      ↓
重点专项突破
      ↓
扫雷补漏
```

---

# 三、每张卡片分为“卡片摘要”和“展开内容”

每张卡片都需要生成两层内容：

```text
Card
├── summary       卡片收起时展示
└── detail        点击后展开的详细内容
```

这样可以避免卡片首页直接堆大量文字。

---

# 四、统一卡片结构

推荐每张卡片生成：

```json
{
  "id": "card_1",
  "order": 1,
  "title": "基础快速过关",
  "subtitle": "一句话说明这一阶段要解决什么",
  "summary": "一句非常简短的阶段摘要",
  "items_count": 4,
  "detail": {
    "goal": "阶段目标",
    "sections": []
  }
}
```

字段说明：

### `id`

固定：

```text
card_1
card_2
card_3
card_4
```

方便前端直接绑定。

### `order`

固定为：

```text
1 / 2 / 3 / 4
```

用于路线顺序。

### `title`

固定四个阶段名称，不由 AI 随意改名。

### `subtitle`

一句话回答：

> **这一阶段为什么存在？**

长度控制在 20～35 个汉字左右。

### `summary`

用于卡片未展开状态。

只写：

> **这一阶段最重要的行动是什么。**

不要把详细任务塞进 summary。

### `items_count`

表示该阶段包含多少个主要任务 / 检查组。

用于前端显示类似：

```text
4 个基础确认
3 个核心任务
1 个专项
3 类扫雷
```

### `detail`

点击卡片后展示的完整内容。

---

# 五、卡片 1：基础快速过关

## 卡片定位

只回答：

> **进入核心复习之前，哪些必要基础必须快速确认？**

不要把它做成完整知识点学习列表。

---

## 卡片摘要

推荐生成：

```text
title:
基础快速过关

subtitle:
先确认后续核心内容真正依赖的定义、条件和基础运算

summary:
快速过一遍必要前置，确认能直接支撑后续核心任务
```

---

## 展开内容结构

```json
{
  "goal": "一句话阶段目标",
  "sections": [
    {
      "type": "quick_check",
      "title": "快速确认",
      "items": []
    },
    {
      "type": "pass_criteria",
      "title": "过关线",
      "items": []
    }
  ]
}
```

### `快速确认`

将多个前置 KP 压缩成短条目：

```text
知识点：需要确认的最关键内容
```

例如结构：

```text
- KP A：确认定义 / 条件
- KP B：确认判断关系
- KP C：确认使用前提
```

每个 KP 最多保留 1～2 个真正必要的确认点。

不要展开：

- practice_type
- risk_tags
- note_missing_items
- 完整知识讲解

---

## 过关线

只保留 2～3 条阶段级标准。

不要为每个前置 KP 单独生成一组标准。

过关线应该回答：

> **什么情况下可以离开这一阶段进入核心冲刺？**

---

# 六、卡片 2：核心冲刺

## 卡片定位

这是行动清单的主体。

只包含：

> **老师本次明确要求重点掌握、重点训练的核心内容。**

---

## 卡片摘要

推荐结构：

```text
title:
核心冲刺

subtitle:
集中处理老师本次点名的核心考点与核心方法

summary:
围绕本次最重要的知识块集中训练，做到会判断、会做、会变式
```

---

## 展开内容

核心冲刺允许包含多个 `task_group`：

```json
{
  "goal": "阶段目标",
  "sections": [
    {
      "type": "task_group",
      "title": "核心任务名称",
      "focus": "本轮真正要抓住什么",
      "tasks": [],
      "pass_criteria": [],
      "reminder": null
    }
  ]
}
```

---

## 核心任务聚合规则

不要默认：

```text
1 KP = 1 核心任务
```

应该优先按训练目标合并。

例如多个 KP 都服务于同一个计算能力，可以合并成：

```text
极限计算冲刺
```

而不是拆成三张高度重复的小任务。

---

## 每个核心任务只保留四类信息

### 1. `title`

任务名称。

应是学生能直接理解的行动主题，例如：

```text
极限计算冲刺
间断点判断
```

不要使用：

```text
任务 1
知识点组 A
```

---

### 2. `focus`

一句话说明：

> **本轮真正抓什么。**

优先来自：

```text
session_focus_items
老师明确强调的方法 / 题型
```

---

### 3. `tasks`

最多 1～3 条。

必须是具体动作。

例如：

```text
□ 每道题先判断方法，再开始计算
□ 完成老师要求的 10 题
```

如果多个动作高度重复，应合并成一句。

---

### 4. `pass_criteria`

最多 2～3 条。

只保留真正能够判断是否完成的标准。

例如：

```text
□ 能根据题目特征选对方法
□ 能独立完成标准题
□ 能处理常见变式
```

---

## 重要限制

不要再输出机械栏目：

```text
任务类型：计算训练、方法选择
```

`practice_type` 是 AI 内部生成任务的依据，不是必须给用户展示的字段。

---

# 七、卡片 3：重点专项突破

## 卡片定位

处理：

- 证明
- 准则
- 综合应用
- 特定流程
- 老师额外强调的专项能力

它和核心冲刺的区别：

```text
核心冲刺
→ 高频 / 核心知识块

重点专项突破
→ 需要独立训练某种能力或流程
```

---

## 卡片摘要

推荐：

```text
title:
重点专项突破

subtitle:
集中突破证明、准则与需要完整流程的重点题型

summary:
把几个需要专项训练的能力单独练透，重点保证流程和书写
```

---

## 展开内容

```json
{
  "goal": "阶段目标",
  "sections": [
    {
      "type": "special_training",
      "title": "专项名称",
      "focus": "专项训练目标",
      "tasks": [],
      "pass_criteria": []
    }
  ]
}
```

---

## 专项聚合规则

优先按“能力”聚合，而不是按 KP 罗列。

例如：

```text
零点定理
夹逼准则
单调有界准则
```

如果老师本次共同强调的是证明和完整推理，可以聚合为：

```text
证明专项
```

再在内容中指出：

```text
重点覆盖：零点定理、夹逼准则、单调有界准则
```

---

# 八、卡片 4：扫雷补漏

## 卡片定位

只回答：

> **最后复习时最容易在哪些地方失分？**

不要再次讲知识。

不要重复核心任务。

---

## 卡片摘要

推荐：

```text
title:
扫雷补漏

subtitle:
最后集中检查概念易混、使用条件和关键流程

summary:
考前统一扫一遍最容易出错的地方，避免会做但丢分
```

---

## 展开内容固定聚合为三类

```json
{
  "goal": "阶段目标",
  "sections": [
    {
      "type": "risk_group",
      "title": "概念辨析",
      "items": []
    },
    {
      "type": "risk_group",
      "title": "条件 / 方法",
      "items": []
    },
    {
      "type": "risk_group",
      "title": "书写 / 流程",
      "items": []
    }
  ]
}
```

---

## 生成依据

综合：

```text
session_error_signal
risk_tags
与本次重点直接相关的 note_missing_items
```

优先级：

```text
老师本次明确提醒
>
核心知识的高风险点
>
与本次重点直接相关的笔记缺项
```

---

## 强制压缩

不要出现：

```text
检查是否混淆 A
检查是否混淆 B
检查是否混淆 C
```

如果属于同类，应聚合为：

```text
概念辨析
- A vs B
- C vs D
```

也不要重复：

```text
使用前检查条件
使用前核适用范围
再次确认使用条件
```

应合并成最具体的一条。

---

# 九、推荐最终 JSON 输出

AI 最终建议直接生成结构化数据：

```json
{
  "action_plan": {
    "cards": [
      {
        "id": "card_1",
        "order": 1,
        "title": "基础快速过关",
        "subtitle": "",
        "summary": "",
        "items_count": 0,
        "detail": {
          "goal": "",
          "sections": []
        }
      },
      {
        "id": "card_2",
        "order": 2,
        "title": "核心冲刺",
        "subtitle": "",
        "summary": "",
        "items_count": 0,
        "detail": {
          "goal": "",
          "sections": []
        }
      },
      {
        "id": "card_3",
        "order": 3,
        "title": "重点专项突破",
        "subtitle": "",
        "summary": "",
        "items_count": 0,
        "detail": {
          "goal": "",
          "sections": []
        }
      },
      {
        "id": "card_4",
        "order": 4,
        "title": "扫雷补漏",
        "subtitle": "",
        "summary": "",
        "items_count": 0,
        "detail": {
          "goal": "",
          "sections": []
        }
      }
    ]
  }
}
```

这样前端可以直接：

```text
cards
→ 渲染 4 张卡片

点击 card.id
→ 展示 card.detail
```

---

# 十、卡片首页文字必须短

卡片未展开状态只显示：

```text
序号
标题
subtitle / summary
任务数量
```

不要把：

- 全部任务
- 全部过关标准
- 全部风险

直接放到卡片首页。

建议控制：

```text
title       ≤ 10 字
subtitle    ≤ 35 字
summary     ≤ 45 字
```

详细内容统一放进：

```text
detail
```

---

# 十一、内容生成压缩规则

## 1. 一个 KP 不等于一张任务卡

四张顶层卡片是固定阶段卡。

KP 只能作为卡片内部内容来源。

---

## 2. 一个字段不等于一句展示文字

例如：

```text
practice_type = calculate
```

不需要显示：

```text
任务类型：计算训练
```

应该直接生成：

```text
每道题先判断方法，再完成计算
```

---

## 3. 相似任务必须合并

如果三个 KP 都需要：

```text
能独立完成标准题
```

在同一个任务组中只保留一次。

---

## 4. summary 只做摘要

不要把详情压进 summary。

---

## 5. detail 才负责完整任务

用户点击卡片之后，才看到：

```text
目标
任务
过关线
风险
```

---

# 十二、四张卡片的内容职责必须互斥

为了避免重复，严格区分：

### 卡片 1：基础快速过关

回答：

> **开始核心复习之前必须确认什么？**

### 卡片 2：核心冲刺

回答：

> **本次最重要的知识和方法要练什么？**

### 卡片 3：重点专项突破

回答：

> **哪些能力需要单独专项练？**

### 卡片 4：扫雷补漏

回答：

> **最后最容易在哪些地方失分？**

同一条内容原则上只进入一个主要卡片。

---

# 十三、禁止生成

不要生成：

```text
本轮复习进度
个人补漏独立模块
前端按钮文案
HTML
CSS
交互代码
折叠状态
动画描述
```

也不要重新生成：

- 全局导航
- 思维导图
- 知识图谱
- 核心知识点讲解

本任务只生成四张行动卡片所需的文字数据。

---

# 十四、质量自检

生成后检查：

### 卡片是否清楚

只看四张卡片标题和摘要，学生是否已经知道完整复习路线？

### 点击后是否有价值

展开某张卡后，是否能明确知道：

```text
我要做什么
重点抓什么
做到什么程度
```

### 是否压缩

是否仍然存在大量：

```text
复述
理解
应用
做题
```

的机械重复？

如果有，需要再次合并。

### 是否重复

同一风险 / 同一任务 / 同一过关标准是否在多个卡片中反复出现？

如果有，只保留在最合适的卡片中。

---

# 十五、最终原则

行动清单最终不是一篇文章，而是四张阶段卡片的数据：

```text
基础快速过关
      ↓
核心冲刺
      ↓
重点专项突破
      ↓
扫雷补漏
```

AI 的任务是：

> **基于 Knowledge Catalog + 老师本次重点文本，把行动内容压缩、聚合并组织成四张卡片。**

前端的任务是：

> **展示四张卡片，并在点击后展示对应 `detail`。**

两者职责分离。
